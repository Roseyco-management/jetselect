---
phase: 09-technical-seo-infrastructure
plan: 01
type: execute
domain: seo
---

<objective>
Implement Schema.org structured data markup to help search engines understand the business entity, service offering, and content structure.

Purpose: Establish rich snippet eligibility and entity recognition in Google Knowledge Graph for "JetSelect" brand and "private jet sales Netherlands" category, improving SERP visibility and click-through rates.

Output: JSON-LD Schema.org markup integrated into HTML head (Organization, LocalBusiness, BreadcrumbList, FAQPage templates) ready for validation and Google Search Console integration in Phase 15.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 8 Context (Content Architecture)
@.planning/phases/08-topical-map-content-architecture/08-01-SUMMARY.md
@.planning/phases/08-topical-map-content-architecture/SITEMAP.md

# Codebase Context
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@improved/index.html

**Tech stack available:** Vanilla HTML/CSS/JS static site, no frameworks, no build process
**Established patterns:**
- Bilingual URL structure: Dutch primary (/kopen/), English secondary (/en/buy/) with hreflang
- Hub-spoke pillar architecture for topical authority
- URL patterns: /kopen/, /gids/, /locaties/, /vliegtuigen/, /zakelijk/, /tools/

**Constraining decisions:**
- Phase 8: 111-page sitemap with bilingual URLs (Dutch/English)
- Phase 8: 5 pillar pages architected with content briefs
- Phase 7: SERP feature strategy targeting 11 featured snippets
- Static site architecture: No server-side rendering, pure client-side

**Issues being addressed:** None (establishing new technical SEO foundation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement core Schema.org JSON-LD markup</name>
  <files>improved/index.html</files>
  <action>
    Add JSON-LD structured data in the HTML `<head>` section for:

    1. **Organization Schema** - Establish JetSelect business entity:
       - @type: "Organization"
       - name: "JetSelect"
       - url: "https://jetselect.nl"
       - logo: Link to primary logo image
       - description: "Premium private jet brokerage serving the Netherlands"
       - address: Netherlands-based (use generic Netherlands address or leave optional)
       - sameAs: Social media profiles (if available, otherwise omit)

    2. **LocalBusiness Schema** - Establish service area:
       - @type: "LocalBusiness"
       - @id: "https://jetselect.nl/#business"
       - name: "JetSelect"
       - priceRange: "€€€€" (luxury positioning)
       - areaServed: Netherlands (geo: NL)
       - serviceType: "Private Jet Brokerage"
       - url: "https://jetselect.nl"

    3. **BreadcrumbList Schema** - Homepage breadcrumb:
       - @type: "BreadcrumbList"
       - itemListElement: Single item for homepage (position 1)
       - name: "Home"
       - item: "https://jetselect.nl"

    **Format:** Use separate `<script type="application/ld+json">` blocks for each schema (easier to maintain than single combined context).

    **What to avoid:** Do NOT use Schema.org microdata attributes (itemscope, itemprop) - JSON-LD in head is Google's preferred format and doesn't pollute HTML. Do NOT add Product schema yet (that's for individual aircraft pages in Phase 12). Do NOT hardcode future page URLs in breadcrumbs (only homepage for now - pillar pages will add their own in Phase 10).
  </action>
  <verify>
    1. Validate JSON-LD syntax: Copy each script block to https://search.google.com/test/rich-results and confirm no errors
    2. Check schema structure: Paste into https://validator.schema.org and verify Organization, LocalBusiness, BreadcrumbList schemas parse correctly
    3. Inspect HTML: View page source and confirm 3 separate `<script type="application/ld+json">` blocks exist in head
  </verify>
  <done>3 Schema.org JSON-LD blocks added to HTML head (Organization, LocalBusiness, BreadcrumbList), all validate without errors in Google Rich Results Test, properly formatted with @context and @type fields</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable FAQPage Schema template</name>
  <files>improved/index.html</files>
  <action>
    Add FAQPage Schema template as HTML comment in `<head>` for use in pillar pages (Phase 10):

    Create a commented-out example showing proper FAQPage structure:
    - @type: "FAQPage"
    - mainEntity: Array of Question objects
    - Each Question contains:
      - @type: "Question"
      - name: Question text
      - acceptedAnswer: Answer object with @type "Answer" and text field

    **Template format:**
    ```html
    <!--
    FAQPage Schema Template (for Phase 10 pillar pages):

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Example question?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Example answer content here."
          }
        }
      ]
    }
    </script>

    Instructions: Copy this template into pillar pages, replace with actual FAQ content from content briefs.
    -->
    ```

    **What to avoid:** Do NOT add actual FAQ content now (content doesn't exist yet - this is just a template for Phase 10). Do NOT use QAPage schema (that's for single Q&A pages, not FAQ pages with multiple questions). Do NOT nest FAQPage inside other schemas (it should be standalone at document level).
  </action>
  <verify>
    1. Check template exists: grep -A 20 "FAQPage Schema Template" improved/index.html shows commented template
    2. Validate template syntax: Copy uncommented JSON from template to https://validator.schema.org and confirm it parses as valid FAQPage schema
    3. Verify instructions: Confirm template includes copy instructions for Phase 10 implementation
  </verify>
  <done>FAQPage Schema template added as HTML comment in head with example Question/Answer structure, validates successfully when uncommented, includes clear instructions for Phase 10 pillar page implementation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 core schemas validate in Google Rich Results Test
- [ ] Schema.org Validator shows no errors for Organization, LocalBusiness, BreadcrumbList
- [ ] FAQPage template exists as comment with valid JSON-LD structure
- [ ] No schema markup breaks HTML rendering or JavaScript functionality
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 3 active Schema.org JSON-LD blocks in HTML head (Organization, LocalBusiness, BreadcrumbList)
- 1 commented FAQPage template ready for Phase 10
- All schemas validate without errors in Google tools
- Static site continues to function normally (no JavaScript errors)
</success_criteria>

<output>
After completion, create `.planning/phases/09-technical-seo-infrastructure/09-01-SUMMARY.md`:

# Phase 9 Plan 1: Schema.org & Structured Data Summary

**[Substantive one-liner - what schemas were implemented, not "schema added"]**

## Accomplishments

- Organization and LocalBusiness schemas establish JetSelect entity
- BreadcrumbList schema ready for pillar page expansion
- FAQPage template created for Phase 10 content production

## Files Created/Modified

- `improved/index.html` - Added 3 Schema.org JSON-LD blocks + FAQPage template

## Decisions Made

[Document any schema structure choices or omissions, or "None - followed Schema.org best practices"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-02-PLAN.md (Meta Tags & International SEO)
</output>
